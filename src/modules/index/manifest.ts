import type { PlannerModuleManifest } from '../../core/registry/plannerModule';

export const indexManifest: PlannerModuleManifest = {
  id: 'index',
  title: 'Индекс',
  description: 'Стартовая служебная страница с ссылками на месяцы и основные разделы.',
  version: '1.0.0',
  sectionType: 'index',
  enabledByDefault: true,
};
